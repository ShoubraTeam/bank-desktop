package com.bank.config;

/**
 * Enum representing the unique keys for the application’s navigation tabs.
 * <p>
 * This enum is used to uniquely identify each tab in the navigation system, ensuring
 * that the correct tab is displayed when the user selects it. The keys are mapped
 * to their corresponding views in the application.
 * </p>
 */
public enum TabKey {
    VIEW_ENTITY,
    ADD_ENTITY,
}
